// Re-exports from octahedral module — kept for backwards-compatible imports.
export {
  OCTA_RESOLUTION as TERRAIN_RESOLUTION,
  octToIndex,
  octDecode,
  octEncode,
  indexToOct,
} from './octahedral.ts';
